name: Update hourly Chacalon context

on:
  # GitHub interpreta el cron en UTC. 17:00 UTC equivale a las 12:00 en Lima
  # durante el horario UTC-5; el proceso se repite al inicio de cada hora.
  schedule:
    - cron: "17 * * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update-context:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Update context file
        run: node scripts/update-context.js

      - name: Commit context when it changed
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add public/data/context.json
          if git diff --cached --quiet; then
            echo "No context changes to commit."
          else
            git commit -m "chore: update hourly Chacalon context"
            git push
          fi
